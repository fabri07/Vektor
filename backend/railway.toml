[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "alembic upgrade head && exec uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2"
restartPolicyType = "on_failure"
