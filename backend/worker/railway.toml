[build]
builder = "dockerfile"
dockerfilePath = "../Dockerfile"

[deploy]
startCommand = "celery -A app.jobs.celery_app worker --loglevel=info --queues=default,scores,notifications,reports,ingestion --concurrency=2"
healthcheckPath = ""
restartPolicyType = "on_failure"
